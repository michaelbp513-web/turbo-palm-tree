# 🚀 N8N Revenue System - Quick Start Guide\n\n## ⏱️ 5-Minute Setup\n\n### Step 1: Prerequisites\n```bash\n# Make sure you have Docker installed\ndocker --version\ndocker-compose --version\n\n# If not installed, get it from:\n# https://www.docker.com/products/docker-desktop\n```\n\n### Step 2: Clone & Navigate\n```bash\ngit clone https://github.com/michaelbp513-web/turbo-palm-tree.git\ncd turbo-palm-tree\n```\n\n### Step 3: Start the System\n```bash\n# Start all services\ndocker-compose up -d\n\n# Check if everything is running\ndocker-compose ps\n```\n\n### Step 4: Access the System\n\n| Service | URL | Login |\n|---------|-----|-------|\n| **N8N Editor** | http://localhost:5678 | (no login) |\n| **Database Admin** | http://localhost:5050 | admin@example.com / admin |\n| **PostgreSQL** | localhost:5432 | revenue_admin / revenue_admin |\n\n## 📊 What's Running?\n\n✅ **N8N** - Workflow automation engine  \n✅ **PostgreSQL** - Database with sample data  \n✅ **PgAdmin** - Database management UI  \n✅ **Redis** (optional) - Caching  \n\n## 🔧 Next Steps\n\n### 1. Load Sample Data\nSample data is already loaded! View it:\n```bash\n# Connect to database\ndocker exec -it n8n_revenue_db psql -U revenue_admin -d n8n_revenue\n\n# View leads\nSELECT COUNT(*) FROM leads;\nSELECT * FROM leads LIMIT 5;\n\n# View conversions\nSELECT * FROM conversions;\n\n# Exit\n\\q\n```\n\n### 2. Set Up Webhooks\n```bash\n# Make the webhook script executable\nchmod +x config/webhook-setup.sh\n\n# Run it\n./config/webhook-setup.sh\n```\n\nWebhook endpoints available:\n- **Lead Capture**: `http://localhost:5678/webhook/lead-capture`\n- **Deal Update**: `http://localhost:5678/webhook/deal-update`\n- **Demo**: `http://localhost:5678/webhook/demo`\n\n### 3. Create a Test Lead\n```bash\ncurl -X POST http://localhost:5678/webhook/lead-capture \\\n  -H 'Content-Type: application/json' \\\n  -d '{\n    \"name\": \"Test User\",\n    \"email\": \"test@example.com\",\n    \"company\": \"Test Company\",\n    \"industry\": \"Technology\",\n    \"budget\": 50000,\n    \"timeline\": \"ASAP\",\n    \"source\": \"webhook_test\"\n  }'\n```\n\nCheck it in the database:\n```bash\ndocker exec -it n8n_revenue_db psql -U revenue_admin -d n8n_revenue\nSELECT * FROM leads WHERE email = 'test@example.com';\n```\n\n### 4. Configure API Integrations\n\n#### HubSpot\n1. Go to http://localhost:5678\n2. Create new workflow\n3. Add HubSpot node\n4. Click \"Create new credentials\"\n5. Paste your HubSpot API key from: https://app.hubspot.com/l/settings/personal-access-keys\n\n#### SendGrid\n1. Get API key from: https://app.sendgrid.com/settings/api_keys\n2. Add SendGrid node in N8N\n3. Paste API key\n\n#### Slack\n1. Create bot at: https://api.slack.com/apps\n2. Get bot token\n3. Add Slack node in N8N\n\n#### Stripe (Payment Tracking)\n1. Get API key from: https://dashboard.stripe.com/apikeys\n2. Add HTTP node to poll Stripe API\n\n## 📈 View Dashboard\n\n### In N8N\n1. Open http://localhost:5678\n2. Click \"Workflows\"\n3. Import from `workflows/` folder\n4. Execute test workflows\n\n### In Database\n```bash\n# Revenue Summary\ndocker exec -it n8n_revenue_db psql -U revenue_admin -d n8n_revenue << 'EOF'\nSELECT \n  COUNT(*) as total_conversions,\n  SUM(revenue) as total_revenue,\n  AVG(revenue) as avg_deal_size\nFROM conversions;\nEOF\n```\n\n## 🧪 Test the System\n\n### Create Workflow Test\n1. Go to http://localhost:5678\n2. Click \"Workflows\" → \"New Workflow\"\n3. Add trigger: \"Webhook\"\n4. Set path: `test`\n5. Add action: \"Log to N8N\"\n6. Click \"Save\"\n7. Copy webhook URL\n8. Test:\n```bash\ncurl -X POST \"http://localhost:5678/webhook/test\" \\\n  -H 'Content-Type: application/json' \\\n  -d '{\"test\": \"data\"}'\n```\n\n## 🔐 Security Setup\n\n### Change Default Passwords\n```bash\n# Edit .env\nPOSTGRES_PASSWORD=your_strong_password\nJWT_SECRET=$(openssl rand -base64 32)\n\n# Restart services\ndocker-compose down\ndocker-compose up -d\n```\n\n### Enable N8N Authentication\n1. Go to http://localhost:5678/admin\n2. Set admin password\n3. All users must login\n\n## 📊 Monitor System\n\n```bash\n# View logs\ndocker-compose logs -f n8n\n\n# View database logs\ndocker-compose logs -f postgres\n\n# Check resource usage\ndocker stats\n```\n\n## 🚀 Deploy to Cloud\n\n### Heroku\n```bash\nchmod +x deployment/heroku-setup.sh\n./deployment/heroku-setup.sh\n```\n\n### AWS\n```bash\nchmod +x deployment/aws-setup.sh\n./deployment/aws-setup.sh\n```\n\n## 🛑 Stop the System\n\n```bash\n# Stop all services (data preserved)\ndocker-compose down\n\n# Stop and remove everything\ndocker-compose down -v\n\n# Remove images too\ndocker-compose down -v --rmi all\n```\n\n## ❓ Troubleshooting\n\n### N8N won't start\n```bash\n# Check logs\ndocker-compose logs n8n\n\n# Restart\ndocker-compose restart n8n\n```\n\n### Database connection error\n```bash\n# Check if database is running\ndocker-compose ps postgres\n\n# Check database logs\ndocker-compose logs postgres\n\n# Restart database\ndocker-compose restart postgres\n```\n\n### Port already in use\n```bash\n# Edit docker-compose.yml\n# Change ports:\n# \"5678:5678\" → \"5679:5678\"\n# \"5432:5432\" → \"5433:5432\"\n```\n\n### Lost data\n```bash\n# Backup before operations\ndocker exec n8n_revenue_db pg_dump -U revenue_admin n8n_revenue > backup.sql\n\n# Restore\ndocker exec -i n8n_revenue_db psql -U revenue_admin n8n_revenue < backup.sql\n```\n\n## 📚 Resources\n\n- [N8N Docs](https://docs.n8n.io/)\n- [N8N Community](https://community.n8n.io/)\n- [PostgreSQL Docs](https://www.postgresql.org/docs/)\n- [Docker Docs](https://docs.docker.com/)\n\n## 🎓 What's Next?\n\n1. ✅ System is running\n2. ✅ Sample data loaded\n3. ✅ Webhooks configured\n4. **→ Import workflows** (next)\n5. **→ Connect CRMs** (after)\n6. **→ Set up email** (then)\n7. **→ Start capturing leads** (finally)\n8. **→ Watch revenue grow** 📈\n\n## 💡 Tips\n\n- **Automate first** - Get the workflows running\n- **Test often** - Use test leads to verify setup\n- **Monitor metrics** - Check dashboards regularly\n- **Iterate fast** - Adjust scoring and sequences based on results\n- **Scale up** - Add more sources as system proves itself\n\n---\n\n**Questions?** Check out the full README.md or visit [https://docs.n8n.io/](https://docs.n8n.io/)\n\n**Ready to start?** Go to http://localhost:5678 now! 🚀\n"
